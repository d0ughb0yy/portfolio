{
  "name": "hugo-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "postcss assets/css/input.css -o static/css/input.css",
    "dev": "npm run build:css && hugo server --renderStaticToDisk",
    "build": "npm run build:css && hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^3.4.17"
  }
}
